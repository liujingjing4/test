name: Force Failed Test

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  fail-test:
    runs-on: ubuntu-latest

    steps:
      - name: This step must fail
        run: |
          echo "Start failure test"
          nonexistent_command_for_test_123456
          echo "This line should not run"
