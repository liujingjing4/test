name: Test Failed Notification

on:
  workflow_dispatch:

jobs:
  fail-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Force workflow failure
        run: |
          echo "This is a test failure for checking GitHub email notifications."
          exit 1
